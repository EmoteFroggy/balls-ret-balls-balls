# fly.toml — Twitch background bot (no HTTP service)

app = "ret-emote-bot"
primary_region = "ord"

[build]

[env]
  NODE_ENV = "production"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
